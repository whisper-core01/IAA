# Shared

This boundary will contain only neutral, versioned contracts, schemas, event
shapes, manifests, and test support shared across kernels.

It must not become a hidden implementation layer or a container for business
logic.

