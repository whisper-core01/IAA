# Doctrine

Doctrine precedes architecture and implementation. It defines what IAA may
become without pretending that intended properties already exist.

The initial doctrine is:

1. kernels are domain-agnostic;
2. organs and components are external and replaceable;
3. all interaction crosses an explicit, versioned contract;
4. a kernel's semantics do not change to accommodate one implementation;
5. evidence bounds every public claim.

